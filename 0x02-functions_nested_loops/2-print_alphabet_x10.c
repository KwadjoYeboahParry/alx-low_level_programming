#include "main.h"
/**
 * main - prints 10 times the alphabets in lowercase
 * followed by a new line
 * return: 0
 */

void print_alphabet_x10(void)

{
	char c;
	int i;

	while (i <= 0)
	{
		for (c = 'a'; c <= 'z'; c++)
		{
			_putchar(c);
		}
		_putchar('\n');
		i++;
	}
}
